package com.example.firebasepushnotificationswithphp.work

import android.app.Application
import android.content.Context
import androidx.core.app.NotificationCompat
import com.example.firebasepushnotificationswithphp.R


class Show_message_notification {


    fun open_noti()
    {
// Create an explicit intent for an Activity in your app
      //  val intent = Intent(this, Hosting_activity::class.java).apply {
      //      flags = Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TASK
     //   }
     //   val pendingIntent: PendingIntent = PendingIntent.getActivity(get, 0, intent, 0)



    }
}